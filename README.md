## Setup
1. Install Node.js v18+ and Python 3.8+
2. Clone repo: `git clone https://github.com/akash564com/fitai-app.git`
3. Install dependencies: `npm install` or `pip install -r requirements.txt`
4. Start app: `npm start` (React Native) / `python app.py` (Python)
